# Playwright
- For local runs, prefer `npm run playwright:test:non-blocking` for concise output; CI uses `npm run playwright:test`.

# Formatting
- Use `npm run biome:fix` after generating code to fix formatting issues.
